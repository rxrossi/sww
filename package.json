{
  "name": "sww",
  "private": true,
  "scripts": {
    "type-check": "lerna run type-check",
    "test": "lerna run test",
    "ci": "pnpm run type-check && pnpm run test"
  },
  "devDependencies": {
    "lerna": "^5.5.4"
  }
}
